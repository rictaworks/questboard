# 除外対象（CLAUDE.md, README.md, SPEC/ 以外）
/.claude/
/.env
/CLIENT/
/DEBUG/
/DELETE/
/ENV/
/TASKS/
/WORK/
/app-ui/
/.agents/
/AGENTS.md
/.next/
/node_modules/
/coverage/
/.env.local
/.env.*.local
*.tsbuildinfo
.vercel
/.playwright-mcp/
/measure_tmp.mjs
next-env.d.ts

# Claudeが実機確認用にリポジトリ直下へ保存するPlaywrightスクリーンショット。
# コード変更ではないため追跡しない（サブディレクトリの画像資産は対象外）。
/*.png
