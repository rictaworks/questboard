RAILS_ENV=development
RAILS_MAX_THREADS=5
RAILS_MASTER_KEY=
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_USER=questboard
POSTGRES_PASSWORD=questboard
POSTGRES_DB=questboard_development
POSTGRES_TEST_DB=questboard_test
DATABASE_URL=
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://questboard.vercel.app
# development環境限定。Codespacesの転送URL（<codespace-name>-<port>.<この値>）越しに
# フロントを開いたときのオリジンをCORS/RequestOriginGuardで許可する
# （lib/development_allowed_origins.rb）。CODESPACE_NAME はCodespaces自身が設定する。
# 本番の環境変数には設定しないこと
CODESPACES_FORWARDING_DOMAIN=app.github.dev
ADMIN_BASIC_AUTH_USERNAME=admin
ADMIN_BASIC_AUTH_PASSWORD=change-me
SECRET_KEY_BASE=replace-with-random-value
X_OAUTH_CLIENT_ID=replace-with-x-client-id
X_OAUTH_REDIRECT_URI=https://app.example.com/auth/x/callback
RECAPTCHA_SECRET_KEY=replace-with-recaptcha-secret
# フォロワー判定サービス（x-follower-gate）への接続。
# 本番のベースURLはリポジトリへ書かない。Railway Variables のみに置く。
# セッションの有効期限（日）。未設定なら7。正の整数以外は起動時に例外。
# フォロワー判定の委譲後、これが降格の担保になる（#258）。
SESSION_EXPIRE_AFTER_DAYS=7
X_FOLLOWER_GATE_BASE_URL=http://localhost:3201
X_FOLLOWER_GATE_CLIENT_ID=replace-with-client-id
X_FOLLOWER_GATE_CREDENTIAL=replace-with-credential
# Sentry error tracking; only activated when RAILS_ENV=production and this is set
SENTRY_DSN=
# Must match the sync-server's own SYNC_SERVER_REDIS_URL / SYNC_SERVER_REDIS_CHANNEL_PREFIX
# (src/sync-server) exactly — both services publish/subscribe on the same Redis instance
# and channel naming so legacy (non-WebSocket) object writes reach connected clients.
SYNC_SERVER_REDIS_URL=
SYNC_SERVER_REDIS_CHANNEL_PREFIX=questboard:sync
# Size of the shared Redis connection pool used to relay legacy (non-WebSocket) object
# writes; defaults to RAILS_MAX_THREADS above when unset.
SYNC_SERVER_REDIS_POOL_SIZE=
